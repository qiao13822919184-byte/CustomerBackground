services:
  app:
    build: .
    restart: unless-stopped
    env_file: .env
    environment:
      HOST: 0.0.0.0
      PORT: 8787
      DATA_DIR: /data
    ports:
      - "127.0.0.1:8787:8787"
    volumes:
      - app_data:/data
    init: true
    stop_grace_period: 30s
volumes:
  app_data:
